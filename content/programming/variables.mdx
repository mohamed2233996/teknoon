---
title: "المتغيرات في البرمجة"
source: "https://harmash.com/tutorials/programming/variables"
source_site: "هرمش (harmash.com)"
imported_at: "2026-07-17T16:07:43.849Z"
---

## مفهوم المتغيرات

المتغير ( Variable ) عبارة عن مكان يتم حجزه في الذاكرة ( RAM ) بهدف تخزين قيمة معينة فيها أثناء تشغيل البرنامج.  
القيمة التي نضعها في المتغير تعتبر قيمة مؤقتة حيث أنه يتم حذفها من الذاكرة بمجرد أن يتوقف البرنامج.

## طريقة تعريف متغير

لتعريف متغير في محرر الكود نكتب `var` و يليها الإسم الذي سنعطيه للمتغير.

في المثال التالي قمنا بتعريف متغير إسمه `name`.

var name;

  

### إعطاء قيمة للمتغير

يمكنك وضع قيمة في المتغير مباشرةً عند إنشائه و يمكنك إعطاؤه قيمة لاحقاً إن أردت.  
لإسناد قيمة للمتغير نقوم بوضع إسمه و يليه رمز المساواة `=` و من ثم القيمة التي نريد وضعها فيه.

في المثال التالي قمنا بتعريف ثلاث متغيرات مع إعطائهم قيم أولية.

var name = "Sami";
var age = 19;
var available = true;

يمكنك تصوّر شكل المتغيرات في الذاكرة كالتالي.

![](/content-images/programming/variables/1784304463833-ax7ah7.PNG)

  

### جلب قيمة المتغير

للحصول على القيمة الموجودة في المتغير نكتب إسمه فقط. فمثلاً لطباعة القيمة الموجودة في متغير نقوم بوضع إسمه في أمر الطباعة `write()`.  

في المثال التالي قمنا بتعريف متغير إسمه `name` و قيمته `"Sami"`.  
بعدها قمنا بطباعة قيمة المتغير.

#### مثال

var name = "Sami";
write("Good morning " + name);

النتيجة

Good morning Sami

[جرب الكود](/algo-editor?file=/tutorials/programming/variables/set-variable-value) [إفهم الكود](/code-stepper?location=/ar/tutorials/programming/variables/set-variable-value)

## القيمة الإفتراضية للمتغيرات `undefined`

في حال قمت بتعريف متغير و لكنك لم تسند إليه أي قيمة فإن محرر الكود سيضع فيه القيمة `undefined` التي تعني أن قيمته غير محددة، بمعنى أنه لم يتم إسناد قيمة إليه بعد.

  

في المثال التالي قمنا بتعريف متغير إسمه `x` ثم حاولنا طباعة قيمته مع أننا لم نعطه قيمة.

#### مثال

var x;
write("x contains: " + x);

النتيجة

x contains: undefined

[جرب الكود](/algo-editor?file=/tutorials/programming/variables/undefined) [إفهم الكود](/code-stepper?location=/ar/tutorials/programming/variables/undefined)

## ماذا يحدث في حال إعادة تعريف المتغير؟

في محرر هرمش إن قمت بإعادة تعريف نفس المتغير مرة ثانية فإنه لا يتم تعريفه من جديد و لا يسبب ذلك أي مشكلة و قيمته تبقى كما هي طالما أنك لم تقوم بتغييرها.

  

في المثال التالي قمنا بتعريف متغير إسمه `x` قيمته `10`.  
بعدها قمنا بإعادة تعريف المتغير `x` مع عدم إعطائه أي قيمة.  
في النهاية قمنا بطباعة قيمته.

#### مثال

var x = 10;
var x;
write("x = " + x);

النتيجة

x = 10

[جرب الكود](/algo-editor?file=/tutorials/programming/variables/redefined) [إفهم الكود](/code-stepper?location=/ar/tutorials/programming/variables/redefined)

  

في بعض لغات البرمجة لا يمكنك تعريف متغيرين بذات الإسم حيث أن كل متغير يجب أن يكون له إسم مختلف. و في حال قمت بتعريف متغيّرين بنفس الإسم فإنه يظهر لك خطأ وقت التشغيل. لهذا ننصحك دائماً بانتقاء أسماء واضحة للمتغيرات و عدم تعريف نفس المتغير أكثر من مرة حتى لا تربك نفسك.

## تمارين شاملة

  

### التمرين الأول

قم بتعريف المتغيرات التالية:

1.  متغير إسمه `a` قيمته `3`.
2.  متغير إسمه `b` قيمته `5`.
3.  متغير إسمه `sum` و قيمته مجموع المتغيرين `a` و `b`.
4.  متغير إسمه `sentence` قيمته `"the sum is: "`.
5.  ثم إطبع محتوايتهم بشكل مرتب.

[الحل و التفسير](/tutorials/programming/variables/ex-1)

  

### التمرين الثاني

أكتب البرنامج السابق بكود أقل، أي بدون تعريف المتغيرات `sum` و `sentence`.

[الحل و التفسير](/tutorials/programming/variables/ex-2)